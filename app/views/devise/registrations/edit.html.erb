<%= link_to :back do %>
  <i class="fas fa-chevron-left back-white"></i>
<% end %>

<div class="form-wrapper col-lg-6">
  <p class="hint">leave it blank if you don't want to change it</p>
  <%= simple_form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put }) do |f| %>
    <%= f.error_notification %>

    <div class="form-inputs">
      <%= f.input :email, 
                  required: true, 
                  placeholder: 'email',
                  autofocus: true %>

      <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
        <p>Currently waiting confirmation for: <%= resource.unconfirmed_email %></p>
      <% end %>

      <% if current_retailer %>
        
        <%= f.input :name,
                    placeholder: "change your name",
                    required: false,
                    input_html: { autocomplete: "new-first-name" } %>
      
      <% else %>
        <%= f.input :first_name,
                    placeholder: "change your first name",
                    required: false,
                    input_html: { autocomplete: "new-first-name" } %>
        <%= f.input :last_name,
                    placeholder: "change your last name",
                    required: false,
                    input_html: { autocomplete: "new-last-name" } %>       
      <% end %>
      
      <%= f.input :password,
                  placeholder: "new password",
                  required: false,
                  input_html: { autocomplete: "new-password" } %>
      <%= f.input :password_confirmation,
                  placeholder: "new password confirmation",
                  required: false,
                  input_html: { autocomplete: "new-password" } %>
      <%= f.input :current_password,
                  placeholder: "current password",
                  hint: "we need your current password to confirm your changes",
                  required: true,
                  input_html: { autocomplete: "current-password" } %>
    </div>

    <div class="form-actions">
      <%= f.button :submit, "Update" %>
    </div>
  <% end %>

  <div class="form-link">
    <p><%= link_to "Cancel my account", registration_path(resource_name), data: { confirm: "Are you sure?" }, method: :delete %></p>
  </div>

</div>